apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
dashboardProviders:
  - name: default
    type: file
    options:
      path: /var/lib/grafana/dashboards
